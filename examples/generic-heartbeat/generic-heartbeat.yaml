substitutions:
  # Compileable placeholder; change these before flashing real hardware.
  board: featheresp32
  flash_size: 4MB
  status_led_pin: GPIO13

esphome:
  name: generic-heartbeat
  friendly_name: Generic Heartbeat

esp32:
  board: ${board}
  flash_size: ${flash_size}
  framework:
    type: arduino

logger:

output:
  - platform: gpio
    id: status_led
    pin: ${status_led_pin}

interval:
  - interval: 2s
    then:
      - logger.log: "heartbeat"
      - output.turn_on: status_led
      - delay: 100ms
      - output.turn_off: status_led
